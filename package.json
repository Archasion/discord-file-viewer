{
  "name": "discord-file-viewer",
  "type": "module",
  "scripts": {
    "deploy": "vercel --prod",
    "test": "vercel dev"
  },
  "dependencies": {
    "@fastify/rate-limit": "^9.1.0",
    "fastify": "^4.26.1"
  },
  "devDependencies": {
    "@types/bun": "^1.0.5"
  },
  "peerDependencies": {
    "typescript": "^5.3.3"
  }
}