{
  "name": "dfv",
  "module": "out/server.js",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "start": "bun out/server.js",
    "test": "bun src/server.ts",
    "build": "bun build src/* --outdir out --minify --target bun"
  },
  "type": "module",
  "dependencies": {
    "@fastify/rate-limit": "^9.1.0",
    "fastify": "^4.26.0"
  }
}